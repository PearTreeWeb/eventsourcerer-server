{% extends 'UI/layout/base.html.twig' %}
{% form_theme form 'form/main_theme.html.twig' %}

{% block content %}
    <div class="flex items-center justify-between mb-6">
        <h1 class="text-xl font-semibold text-slate-800 dark:text-slate-100">View Event</h1>
        <a class="button button-secondary" href="{{ path('events') }}">Back to Events</a>
    </div>
    <div class="bg-white rounded-xl border border-slate-200 shadow-sm p-6 max-w-2xl">
        {{ form_start(form) }}
            <div class="mb-5">
                {{ form_label(form.name, 'Event Name', {label_attr: {class: 'form-label'}}) }}
                {{ form_widget(form.name, { attr: { class: 'form-input' }} ) }}
            </div>
            <div class="mb-4">
                {{ form_widget(form.properties) }}
            </div>
        {{ form_end(form) }}
    </div>
{% endblock %}
